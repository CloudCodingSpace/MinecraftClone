{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppdbg",
            "externalConsole": true,
            "cwd": "${workspaceFolder}",
            "request": "launch",
            "name": "MinecraftClone Launch",
            "program": "${workspaceFolder}/bin/MinecraftClone.exe",
            "stopAtEntry": false
        }
    ]
}